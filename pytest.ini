[pytest]
markers =
    api: tests that call Gemini API
    slow: longer-running integration tests
addopts = -q
filterwarnings =
    ignore:.*_UnionGenericAlias.*:DeprecationWarning:google\.genai\.types
